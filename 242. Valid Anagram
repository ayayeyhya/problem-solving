class Solution {
public:
    bool isAnagram(string s, string t) {
        bool check=false;
        sort(s.begin(), s.end());
        sort(t.begin(), t.end());
        if(s==t){check=true;}
        return check;
    }
};
